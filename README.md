# HardCode_test

Установка библиотек
pip install requests 
pip install random
pip install pytest
pip install pytest-html


Запуск тестов
pytest

Генерация отчета
pytest --html=report.html --self-contained-html

